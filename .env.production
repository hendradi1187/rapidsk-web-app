# ─── Build PRODUKSI / SERVER ──────────────────────────────────────────
# Dimuat otomatis oleh `npm run build` (mode=production) dan di-BAKE ke bundle.
# JANGAN pakai localhost: URL ini di-resolve oleh BROWSER pengguna, bukan server.
# Arahkan ke IP server tempat fabric (:8184) & node (:8000) berjalan.
VITE_API_BASE_URL=http://45.158.126.171:8185/api/v1
VITE_NODE_BASE_URL=http://45.158.126.171:8000

# Keycloak OFF (LOCAL JWT) — biarkan kosong.
# VITE_KEYCLOAK_URL=
