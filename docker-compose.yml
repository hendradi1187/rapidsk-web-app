services:
  frontend:
    container_name: dataspace-frontend
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://45.158.126.171:8182}
    ports:
      - "${APP_PORT:-8183}:80"
    restart: unless-stopped
