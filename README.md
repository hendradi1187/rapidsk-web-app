## Docker

Run in one command:

```powershell
docker compose up --build -d
```

Frontend will be available at `http://localhost:8183` by default.

If you need a different host port or API base URL, copy `.env.example` to `.env` and edit the values before running Docker.
