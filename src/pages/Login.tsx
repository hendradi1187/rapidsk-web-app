import LoginPage from "@/components/login/LoginPage";

/** Halaman /login — komposisi komponen di @/components/login. Logika auth ada di LoginPage. */
const Login = () => <LoginPage />;

export default Login;
